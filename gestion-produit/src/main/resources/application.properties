spring.application.name=gestion-produit
# Server Configuration
server.port=8087

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/pi2025

# Configuration SMTP pour Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=zaougaeya@gmail.com
spring.mail.password=deme ywuc fakt svgl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

# Enable automatic registration and fetch registry
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
nominatim.base-url=https://nominatim.openstreetmap.org
nominatim.user-agent=ProduitService/1.0 (zaougaeya@gmail.com)  # Format requis
nominatim.request.delay=2000  

spring.cloud.compatibility-verifier.enabled=false
management.endpoints.web.exposure.include=health,info,metrics
spring.jackson.mapping.force-lazy-loading=false
spring.jackson.serialization.fail-on-empty-beans=false
spring.security.enabled=false
gestion-user.url=http://localhost:8081
